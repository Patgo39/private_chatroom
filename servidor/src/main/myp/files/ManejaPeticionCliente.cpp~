#include "ManejaPeticionCliente.h"

std::string ManejaPeticionCliente::convierteACadena(Json::Value){
  Json::StreamWriterBuilder builder;
  const std::string output = Json::writeString(builder, objetoJson);

  return output;
}

Json::Value ManejaPeticionCliente::convierteAJson(char buffer[]){
  Json::Value bufferJson;
  Json::CharReaderBuilder builder;
  JSONCPP_STRING errs;
  std::istringstream s(buffer);

  parseFromStream(builder, s, &bufferJson, &errs);

  return bufferJson;
}

std::string ManejaPeticionCliente::manejaPeticion(char buffer[]){
  Json::Value peticion = convierteAJson(buffer);

  if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::IDENTIFY)){
    
  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::STATUS)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::USERS)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::TEXT)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::PUBLIC_TEXT)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::NEW_ROOM)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::INVITE)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::JOIN_ROOM)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::ROOM_USERS)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::ROOM_TEXT)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::LEAVE_ROOM)){

  }else if(peticion["type"] == TipoCliente::getString(TipoCliente::Tipo::DISCONNECT)){

  }
}

std::string manejaIdentificacion(char[] buffer, std::vector<string> vect){
  
}
